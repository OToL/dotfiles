require("ebeau.core")           -- load base global utilities, settings and keymaps
require("ebeau.plugins_index")  -- register plugins to enable
require("ebeau.lazy")           -- kickstart plugins loading & initialization
